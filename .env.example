# Copy this file to ".env" and adjust values. The real .env is gitignored.
# These vars drive both docker-compose (the Postgres container) and the Python app.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=churn_db
DB_USER=churn
DB_PASSWORD=change_me_locally
